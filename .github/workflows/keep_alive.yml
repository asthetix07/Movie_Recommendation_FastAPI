name: Keep Streamlit App Awake

on:
  schedule:
    # Runs every 6 hours to prevent Streamlit Community Cloud from sleeping
    - cron: '0 */6 * * *'
  workflow_dispatch: # Allows manual trigger from GitHub Actions UI

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: |
          curl -s -L -b /tmp/cookies.txt -c /tmp/cookies.txt "https://cinewatch-vercel.streamlit.app" > /dev/null
          echo "Streamlit app pinged successfully at $(date)"

